#include <stdio.h>
#include <math.h>


int main()
{
	/*int a;
	int b;

	printf("enter the two digit number\n");
	printf("enter the first digit: ");
	scanf("%d", &a);
	printf("enter the second digit: ");
	scanf("%d", &b);
	double c = sqrt((a*a) + (b*b));
	printf("your angle value is :%f", c);
	putchar('\n');
	*/

	char x[]="putchar";
		int i = 0;
	while(i <= 6)
	{ i++;
	putchar(x[i]);
	}
	putchar('\n');
	return 0;

}
